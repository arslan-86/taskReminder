<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Task Reminder</title>
</head>
<body>
    <header>
        <h1>Task Reminder</h1>
    </header>
    <div class="container">
        <form class="inputContainer" action="/task" method="post" id="form">
            <label for="text">Task Name:</label><br>
            <input type="text" name="text" id="text" placeholder="Enter Task" required>
            <br>
            <label for="dateTime">Select Deadline:</label><br>
            <input type="datetime-local" name="dateTime" id="dateTime" required  >
            <br>
            <button type="submit" id="btn">Submit</button>
        </form>
        <div class="outputContainer">
            <h3>All Tasks</h3>
            
            <% data.forEach(item => {%> 
                <div class="tasks">
                <p id="taskName"><%= item.task%></p>
                <p id="taskDeadline">By: <%= item.deadline%></p>
               </div>
            <%})%>
        </div>
        </div>
        
    </div>

    <script>
        // const button = document.getElementById('btn');

        // button.addEventListener('click', (e) => {
        //     e.preventDefault();
        //     const textInput = document.getElementById('text');
        //     const dateTime  = document.getElementById("dateTime");
        //     const deadline = new Date(dateTime.value);
        //     const result = document.getElementById('outputPara');

        //     result.innerHTML = `${textInput.value}  Deadline:<span id="outputDate">${deadline.toLocaleDateString()}</span>`;

        //     document.getElementById('form').submit();
        // })

    </script>
</body>
</html>